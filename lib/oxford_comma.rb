def oxford_comma(array)
  if(array.length >2)
  puts  array.join(", ")
  puts array.class
    #array.split(" ")
  #  lastword=array.pop
    #array.push("and")
  #  array.push(lastword)
     array.join
else

array.join(" and ")
end
end


array=["kiwi", "durian", "starfruit"]
oxford_comma(array)
